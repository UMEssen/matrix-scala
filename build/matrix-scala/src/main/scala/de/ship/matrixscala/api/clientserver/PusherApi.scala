/**
  * <h1>matrix-scala</h1>
  * @version 1.0.0
  * @author kirill.sokol@uk-essen.de
  *
  * This file was generated.
  *
  * DO NOT EDIT THIS FILE. Any changes to this file will be overwritten
  * the next time the code is generated. If you need to modify the code
  * generated by this tool, make your changes to the generator script
  * and regenerate the code.
  */

package de.ship.matrixscala.api.clientserver

import de.ship.matrixscala.model.Definitions
import de.ship.matrixscala.model.clientserver.PusherData._
import de.ship.matrixscala.json.clientserver.PusherJsonFormats._
import de.ship.matrixscala.json.DefinitionFormats._
import de.ship.matrixscala.core._
import de.ship.matrixscala.core.ResponseMappings._
import de.ship.matrixscala.core.QueryBuilding._
import de.ship.matrixscala.core.HeaderBuilding._
import akka.http.scaladsl.model.HttpMethods
import spray.json.{JsObject, JsValue, RootJsonFormat}
import spray.json.DefaultJsonProtocol._

/**
  * @see "defined in ./build/matrix-spec/data/api/client-server/pusher.yaml"
  */
object PusherApi {

  /**
    * <h1>getPushers</h1>
    *
    * Gets all currently active pushers for the authenticated user.
    */
  def getPushers(
  ): ApiRequest[AccessTokenAuthentication, JsObject, GetPushers.OK] =
    ApiRequest(
      endpoint = "/_matrix/client/v3/pushers",
      httpMethod = HttpMethods.GET,
      body = JsObject(),
      bodyFormat = implicitly[RootJsonFormat[JsObject]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[GetPushers.OK]])
      ),
      inQuery = Map.empty,
      inPath = Map.empty,
      inHeader = Map.empty
    )

  /**
    * <h1>postPusher</h1>
    *
    * This endpoint allows the creation, modification and deletion of [pushers](/client-server-api/#push-notifications)
    * for this user ID. The behaviour of this endpoint varies depending on the
    * values in the JSON body.
    *
    * If `kind` is not `null`, the pusher with this `app_id` and `pushkey`
    * for this user is updated, or it is created if it doesn't exist. If
    * `kind` is `null`, the pusher with this `app_id` and `pushkey` for this
    * user is deleted.
    */
  def postPusher(
      body: PostPusher.Body
  ): ApiRequest[AccessTokenAuthentication, PostPusher.Body, JsObject] =
    ApiRequest(
      endpoint = "/_matrix/client/v3/pushers/set",
      httpMethod = HttpMethods.POST,
      body = body,
      bodyFormat = implicitly[RootJsonFormat[PostPusher.Body]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[JsObject]])
      ),
      inQuery = Map.empty,
      inPath = Map.empty,
      inHeader = Map.empty
    )

}
