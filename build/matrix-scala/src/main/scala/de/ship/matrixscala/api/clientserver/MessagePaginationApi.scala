/**
  * <h1>matrix-scala</h1>
  * @version 1.0.0
  * @author kirill.sokol@uk-essen.de
  *
  * This file was generated.
  *
  * DO NOT EDIT THIS FILE. Any changes to this file will be overwritten
  * the next time the code is generated. If you need to modify the code
  * generated by this tool, make your changes to the generator script
  * and regenerate the code.
  */

package de.ship.matrixscala.api.clientserver

import de.ship.matrixscala.model.Definitions
import de.ship.matrixscala.model.clientserver.MessagePaginationData._
import de.ship.matrixscala.json.clientserver.MessagePaginationJsonFormats._
import de.ship.matrixscala.json.DefinitionFormats._
import de.ship.matrixscala.core._
import de.ship.matrixscala.core.ResponseMappings._
import de.ship.matrixscala.core.QueryBuilding._
import de.ship.matrixscala.core.HeaderBuilding._
import akka.http.scaladsl.model.HttpMethods
import spray.json.{JsObject, JsValue, RootJsonFormat}
import spray.json.DefaultJsonProtocol._

/**
  * @see "defined in ./build/matrix-spec/data/api/client-server/message_pagination.yaml"
  */
object MessagePaginationApi {

  /**
    * <h1>getRoomEvents</h1>
    *
    * This API returns a list of message and state events for a room. It uses
    * pagination query parameters to paginate history in the room.
    *
    * Note*: This endpoint supports lazy-loading of room member events. See
    * [Lazy-loading room members](/client-server-api/#lazy-loading-room-members) for more information.
    *
    * @param roomId The room to get events from.
    *
    * @param from The token to start returning events from. This token can be obtained
    * from a `prev_batch` or `next_batch` token returned by the `/sync` endpoint,
    * or from an `end` token returned by a previous request to this endpoint.
    *
    * This endpoint can also accept a value returned as a `start` token
    * by a previous request to this endpoint, though servers are not
    * required to support this. Clients should not rely on the behaviour.
    *
    * If it is not provided, the homeserver shall return a list of messages
    * from the first or last (per the value of the `dir` parameter) visible
    * event in the room history for the requesting user.
    *
    * @param to The token to stop returning events at. This token can be obtained from
    * a `prev_batch` or `next_batch` token returned by the `/sync` endpoint,
    * or from an `end` token returned by a previous request to this endpoint.
    *
    * @param dir The direction to return events from. If this is set to `f`, events
    * will be returned in chronological order starting at `from`. If it
    * is set to `b`, events will be returned in *reverse* chronological
    * order, again starting at `from`.
    *
    * @param limit The maximum number of events to return. Default: 10.
    *
    * @param filter A JSON RoomEventFilter to filter returned events with.
    */
  def getRoomEvents(
      roomId: String,
      from: Option[String] = None,
      to: Option[String] = None,
      dir: String,
      limit: Option[Int] = None,
      filter: Option[String] = None
  ): ApiRequest[AccessTokenAuthentication, JsObject, GetRoomEvents.OK] =
    ApiRequest(
      endpoint = "/_matrix/client/v3/rooms/{roomId}/messages",
      httpMethod = HttpMethods.GET,
      body = JsObject(),
      bodyFormat = implicitly[RootJsonFormat[JsObject]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[GetRoomEvents.OK]])
      ),
      inQuery = buildQuery(
        fromQueryEntry[Option[String]]("from"   -> from),
        fromQueryEntry[Option[String]]("to"     -> to),
        fromQueryEntry[String]("dir"            -> dir),
        fromQueryEntry[Option[Int]]("limit"     -> limit),
        fromQueryEntry[Option[String]]("filter" -> filter)
      ),
      inPath = Map(
        "roomId" -> roomId
      ),
      inHeader = Map.empty
    )

}
