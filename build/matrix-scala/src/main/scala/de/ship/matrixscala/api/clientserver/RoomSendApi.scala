/**
  * <h1>matrix-scala</h1>
  * @version 1.0.0
  * @author kirill.sokol@uk-essen.de
  *
  * This file was generated.
  *
  * DO NOT EDIT THIS FILE. Any changes to this file will be overwritten
  * the next time the code is generated. If you need to modify the code
  * generated by this tool, make your changes to the generator script
  * and regenerate the code.
  */

package de.ship.matrixscala.api.clientserver

import de.ship.matrixscala.model.Definitions
import de.ship.matrixscala.model.clientserver.RoomSendData._
import de.ship.matrixscala.json.clientserver.RoomSendJsonFormats._
import de.ship.matrixscala.json.DefinitionFormats._
import de.ship.matrixscala.core._
import de.ship.matrixscala.core.ResponseMappings._
import de.ship.matrixscala.core.QueryBuilding._
import de.ship.matrixscala.core.HeaderBuilding._
import akka.http.scaladsl.model.HttpMethods
import spray.json.{JsObject, JsValue, RootJsonFormat}
import spray.json.DefaultJsonProtocol._

/**
  * @see "defined in ./build/matrix-spec/data/api/client-server/room_send.yaml"
  */
object RoomSendApi {

  /**
    * <h1>sendMessage</h1>
    *
    * This endpoint is used to send a message event to a room. Message events
    * allow access to historical events and pagination, making them suited
    * for "once-off" activity in a room.
    *
    * The body of the request should be the content object of the event; the
    * fields in this object will vary depending on the type of event. See
    * [Room Events](/client-server-api/#room-events) for the m. event specification.
    *
    * @param roomId The room to send the event to.
    *
    * @param eventType The type of event to send.
    *
    * @param txnId The [transaction ID](/client-server-api/#transaction-identifiers) for this event. Clients should generate an
    * ID unique across requests with the same access token; it will be
    * used by the server to ensure idempotency of requests.
    */
  def sendMessage(
      body: JsObject,
      roomId: String,
      eventType: String,
      txnId: String
  ): ApiRequest[AccessTokenAuthentication, JsObject, SendMessage.OK] =
    ApiRequest(
      endpoint = "/_matrix/client/v3/rooms/{roomId}/send/{eventType}/{txnId}",
      httpMethod = HttpMethods.PUT,
      body = body,
      bodyFormat = implicitly[RootJsonFormat[JsObject]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[SendMessage.OK]])
      ),
      inQuery = Map.empty,
      inPath = Map(
        "roomId"    -> roomId,
        "eventType" -> eventType,
        "txnId"     -> txnId
      ),
      inHeader = Map.empty
    )

}
