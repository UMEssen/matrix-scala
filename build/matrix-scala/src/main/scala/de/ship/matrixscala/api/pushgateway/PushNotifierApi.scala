/**
  * <h1>matrix-scala</h1>
  * @version 1.0.0
  * @author kirill.sokol@uk-essen.de
  *
  * This file was generated.
  *
  * DO NOT EDIT THIS FILE. Any changes to this file will be overwritten
  * the next time the code is generated. If you need to modify the code
  * generated by this tool, make your changes to the generator script
  * and regenerate the code.
  */

package de.ship.matrixscala.api.pushgateway

import de.ship.matrixscala.model.Definitions
import de.ship.matrixscala.model.pushgateway.PushNotifierData._
import de.ship.matrixscala.json.pushgateway.PushNotifierJsonFormats._
import de.ship.matrixscala.json.DefinitionFormats._
import de.ship.matrixscala.core._
import de.ship.matrixscala.core.ResponseMappings._
import de.ship.matrixscala.core.QueryBuilding._
import de.ship.matrixscala.core.HeaderBuilding._
import akka.http.scaladsl.model.HttpMethods
import spray.json.{JsObject, JsValue, RootJsonFormat}
import spray.json.DefaultJsonProtocol._

/**
  * @see "defined in ./build/matrix-spec/data/api/push-gateway/push_notifier.yaml"
  */
object PushNotifierApi {

  /**
    * <h1>notify</h1>
    *
    * This endpoint is invoked by HTTP pushers to notify a push gateway about
    * an event or update the number of unread notifications a user has.
    * In the former case it will contain selected information about the event.
    * In either case it may contain numeric counts of the number of unread
    * events of different types the user has. The counts may be sent along
    * with a notification about an event or by themselves.
    *
    * Notifications about a particular event will normally cause the user to be
    * alerted in some way. It is therefore necessary to perform duplicate
    * suppression for such notifications using the `event_id` field to avoid
    * retries of this HTTP API causing duplicate alerts. The operation of
    * updating counts of unread notifications should be idempotent and
    * therefore do not require duplicate suppression.
    *
    * Notifications are sent to the URL configured when the pusher is created.
    * This means that the HTTP path may be different depending on the push
    * gateway.
    */
  def notify(
      body: Notify.Body
  ): ApiRequest[NoAuthentication, Notify.Body, Notify.OK] =
    ApiRequest(
      endpoint = "/_matrix/push/v1/notify",
      httpMethod = HttpMethods.POST,
      body = body,
      bodyFormat = implicitly[RootJsonFormat[Notify.Body]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[Notify.OK]])
      ),
      inQuery = Map.empty,
      inPath = Map.empty,
      inHeader = Map.empty
    )

}
