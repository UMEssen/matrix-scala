/**
  * <h1>matrix-scala</h1>
  * @version 1.0.0
  * @author kirill.sokol@uk-essen.de
  *
  * This file was generated.
  *
  * DO NOT EDIT THIS FILE. Any changes to this file will be overwritten
  * the next time the code is generated. If you need to modify the code
  * generated by this tool, make your changes to the generator script
  * and regenerate the code.
  */

package de.ship.matrixscala.api.identity

import de.ship.matrixscala.model.Definitions
import de.ship.matrixscala.model.identity.V2LookupData._
import de.ship.matrixscala.json.identity.V2LookupJsonFormats._
import de.ship.matrixscala.json.DefinitionFormats._
import de.ship.matrixscala.core._
import de.ship.matrixscala.core.ResponseMappings._
import de.ship.matrixscala.core.QueryBuilding._
import de.ship.matrixscala.core.HeaderBuilding._
import akka.http.scaladsl.model.HttpMethods
import spray.json.{JsObject, JsValue, RootJsonFormat}
import spray.json.DefaultJsonProtocol._

/**
  * @see "defined in ./build/matrix-spec/data/api/identity/v2_lookup.yaml"
  */
object V2LookupApi {

  /**
    * <h1>getHashDetails</h1>
    *
    * Gets parameters for hashing identifiers from the server. This can include
    * any of the algorithms defined in this specification.
    */
  def getHashDetails(
  ): ApiRequest[AccessTokenAuthentication, JsObject, GetHashDetails.OK] =
    ApiRequest(
      endpoint = "/_matrix/identity/v2/hash_details",
      httpMethod = HttpMethods.GET,
      body = JsObject(),
      bodyFormat = implicitly[RootJsonFormat[JsObject]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[GetHashDetails.OK]])
      ),
      inQuery = Map.empty,
      inPath = Map.empty,
      inHeader = Map.empty
    )

  /**
    * <h1>lookupUsersV2</h1>
    *
    * Looks up the set of Matrix User IDs which have bound the 3PIDs given, if
    * bindings are available. Note that the format of the addresses is defined
    * later in this specification.
    */
  def lookupUsersV2(
      body: LookupUsersV2.Body
  ): ApiRequest[AccessTokenAuthentication, LookupUsersV2.Body, LookupUsersV2.OK] =
    ApiRequest(
      endpoint = "/_matrix/identity/v2/lookup",
      httpMethod = HttpMethods.POST,
      body = body,
      bodyFormat = implicitly[RootJsonFormat[LookupUsersV2.Body]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[LookupUsersV2.OK]])
      ),
      inQuery = Map.empty,
      inPath = Map.empty,
      inHeader = Map.empty
    )

}
