/**
  * <h1>matrix-scala</h1>
  * @version 1.0.0
  * @author kirill.sokol@uk-essen.de
  *
  * This file was generated.
  *
  * DO NOT EDIT THIS FILE. Any changes to this file will be overwritten
  * the next time the code is generated. If you need to modify the code
  * generated by this tool, make your changes to the generator script
  * and regenerate the code.
  */

package de.ship.matrixscala.api.clientserver

import de.ship.matrixscala.model.Definitions
import de.ship.matrixscala.model.clientserver.PeekingEventsData._
import de.ship.matrixscala.json.clientserver.PeekingEventsJsonFormats._
import de.ship.matrixscala.json.DefinitionFormats._
import de.ship.matrixscala.core._
import de.ship.matrixscala.core.ResponseMappings._
import de.ship.matrixscala.core.QueryBuilding._
import de.ship.matrixscala.core.HeaderBuilding._
import akka.http.scaladsl.model.HttpMethods
import spray.json.{JsObject, JsValue, RootJsonFormat}
import spray.json.DefaultJsonProtocol._

/**
  * @see "defined in ./build/matrix-spec/data/api/client-server/peeking_events.yaml"
  */
object PeekingEventsApi {

  /**
    * <h1>peekEvents</h1>
    *
    * This will listen for new events related to a particular room and return
    * them to the caller. This will block until an event is received, or until
    * the `timeout` is reached.
    *
    * This API is the same as the normal `/events` endpoint, but can be
    * called by users who have not joined the room.
    *
    * Note that the normal `/events` endpoint has been deprecated. This
    * API will also be deprecated at some point, but its replacement is not
    * yet known.
    *
    * @param from The token to stream from. This token is either from a previous
    * request to this API or from the initial sync API.
    *
    * @param timeout The maximum time in milliseconds to wait for an event.
    *
    * @param room_id The room ID for which events should be returned.
    */
  def peekEvents(
      from: Option[String] = None,
      timeout: Option[Int] = None,
      room_id: Option[String] = None
  ): ApiRequest[AccessTokenAuthentication, JsObject, PeekEvents.OK] =
    ApiRequest(
      endpoint = "/_matrix/client/v3/events ",
      httpMethod = HttpMethods.GET,
      body = JsObject(),
      bodyFormat = implicitly[RootJsonFormat[JsObject]],
      responseMappings = Seq(
        SingleResponseMapping(200, implicitly[RootJsonFormat[PeekEvents.OK]])
      ),
      inQuery = buildQuery(
        fromQueryEntry[Option[String]]("from"    -> from),
        fromQueryEntry[Option[Int]]("timeout"    -> timeout),
        fromQueryEntry[Option[String]]("room_id" -> room_id)
      ),
      inPath = Map.empty,
      inHeader = Map.empty
    )

}
